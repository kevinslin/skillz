# Changelog

## Unreleased

- changed: remove rendered file sync; sync now copies skill directories only (NOT_IN_SPEC)
- changed: remove configurable skills-heading field from config, schema, tests, and docs (NOT_IN_SPEC)
- feat: add `skillz watch` command with debounced syncing and configurable polling interval
