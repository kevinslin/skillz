# Changelog

## Unreleased

- changed: remove `skillsSectionName`; prompt sync now uses the canonical `## Skills` section heading (NOT_IN_SPEC)
- feat: add `skillz watch` command with debounced syncing and configurable polling interval
